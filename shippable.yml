language: python

python:
  - 2.7

build:
  pre_ci_boot:
    image_name: aksum/docker-debian8-ansible
    image_tag: latest
    pull: true

  pre_ci:
    - pip install molecule

  ci:
    - molecule test
